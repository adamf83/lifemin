confirm_item:
  fields:
    entry_id:
      required: true
      selector:
        config_entry:
          integration: admin_inbox
    item_id:
      required: true
      example: "01J9A..."
      selector:
        text:

reject_item:
  fields:
    entry_id:
      required: true
      selector:
        config_entry:
          integration: admin_inbox
    item_id:
      required: true
      example: "01J9A..."
      selector:
        text:

reconcile:
  fields:
    entry_id:
      required: true
      selector:
        config_entry:
          integration: admin_inbox

upload_document:
  fields:
    entry_id:
      required: true
      selector:
        config_entry:
          integration: admin_inbox
    file:
      required: true
      selector:
        file:
          accept: "image/*,.pdf,application/pdf"
    sender:
      required: false
      example: "British Gas"
      selector:
        text:
    subject:
      required: false
      example: "Energy bill"
      selector:
        text:
    notes:
      required: false
      example: "Found this in the post"
      selector:
        text:
          multiline: true
